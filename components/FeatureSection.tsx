// This component is now integrated directly into App.tsx for the sticky scroll layout effect.
// Keeping file to prevent errors if imported elsewhere, but it renders nothing.
import React from 'react';
const FeatureSection: React.FC<any> = () => null;
export default FeatureSection;